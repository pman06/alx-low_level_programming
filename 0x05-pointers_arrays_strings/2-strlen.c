#include <string.h>
#include <stdlib.h>
#include "main.h"
#include <stdio.h>
/**
 * _strlen - Function to return string length.
 * @i: string
 * Return: length os string
 */
int _strlen(char *i)
{
	int length = strlen(i);

	return (length);
}
